/**
 * 定义 Zego 插件类型枚举，用于区分不同的插件功能。
 */
export declare enum ZegoPluginType {
    /**
     * 信号处理插件，用于消息传递和通知。
     */
    Signling = 0,
    /**
     * iOS 的呼叫界面插件，提供系统级的呼叫界面集成。
     */
    CallKit = 1
}
